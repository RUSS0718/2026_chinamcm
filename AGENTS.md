# AGENTS.md

本文件适用于整个仓库。人员分工、阶段材料和时间节点以 `README.md` 为准；本文件只保留 Agent 必须长期执行的稳定规则。

## 基本原则

- 修改前先说明已知事实、必要假设、真实入口、修改范围、验证方式和明确不会执行的操作。
- 不同理解会改变模型或结果时先确认，不默认选择方便的解释。
- 只做当前任务必需的修改，不顺手重构、格式化或删除无关内容。
- 保留无关的未提交改动，不覆盖、清理、暂存或提交它们。
- 明确区分已验证事实、合理推测、建议和暂时无法验证的信息。

## 学术诚信

- 不得编造数据、运行结果、图表、指标、模型优势、参考文献或引用。
- 未实际运行的命令不得写成“已通过”；空目录、占位代码、结构检查或单元测试不得写成模型效果证据。
- 数字、日期、引用和外部事实尽可能核对来源；无法核对时明确标注。
- 当届赛事官方规则、数据许可和学术诚信要求优先于仓库约定。

## 目录边界

- `data/raw/` 是只读输入，不得由程序覆盖或原地清洗。
- 清洗数据和特征写入 `data/processed/`；正式结果写入 `outputs/qN/`；论文可用材料写入 `paper/`。
- `tmp/` 只用于本地临时文件，不纳入阶段 commit。
- 使用 `python -m src.Q1`、`src/q2.py`、`src/q3.py` 或 `requirements.txt` 前先检查实际内容；仅有占位说明的文件不得视为已实现、已运行或可复现。
- 未通过数据来源、实际运行和复核的文件，不得迁入 `outputs/qN/final/` 或写成论文确定结论。

## 数据与模型

- 清洗必须记录处理前后行数、列数、排除规则和原因，不得为了更好的图形或指标静默删除数据。
- 变量、公式、指标方向、单位、参数、随机种子和软件环境必须在代码、报告和论文中一致。
- V1 至少比较一个主方案和一个基线或备选方案；不得为满足数量虚构不适用的模型。
- V2 必须给出公式、参数来源、真实入口、实际执行命令和阶段运行证据。
- V3 必须给出完整结果表、直接结论、关键数字和与模型类型匹配的检验，不得只提供截图或“效果较好”之类定性表述。
- 每个关键数字必须能追溯到结果表、代码入口和运行输出。

## 制图

- 论文数据图使用 `nature-figure` skill，流程图和模型框架图使用 `drawio-skill`。
- 面向本项目论文和验收交付的正式图，标题、坐标轴、图例、图注和说明文字默认使用中文；模型代号、变量符号和通用指标缩写（如 Q1-BT、P1、SA、HPWL）保留规范写法。除非用户明确要求英文版，不得用英文描述替代中文说明。
- 制图前明确结论、数据来源、字段、排除规则、单位、图注和导出尺寸。
- 数据图选定 Python 或 R 后，同一张图的绘制、预览、导出和视觉检查使用同一后端。
- 正式图至少导出 SVG 或 PDF，并提供 PNG 预览；在最终尺寸下检查标签、单位、图例、图注和文字。
- Git 默认只提交论文采用的最终图和对应绘图数据表。制图脚本、中间预览、QA 文件和重复格式保留在本地，除非用户明确要求提交。

## 阶段门槛

- 阶段状态为 `DRAFT → REVIEWING → VERIFIED → FINAL`。Agent 可维护前两种状态，但不得代替复核人标记 `VERIFIED` 或 `FINAL`。
- 阶段报告记录问题编号、状态、负责人、复核人、更新时间、实际命令、验证结果和未解决问题。
- 后一问改变前一问的数据、参数、公式或关键数字时，说明影响范围，重新执行受影响的检验，并退回对应阶段复核。

## Git 规则

- 本项目中的“提交”指 Git commit，不等于 push、合并或最终发布。
- Agent 只能在用户明确要求后创建 commit。创建前展示范围和验证结果，只暂存当前阶段文件。
- push 需要用户单独明确授权；不得自行改写历史、强制推送、清理工作区或提交无关改动。
- 阶段 commit 使用 `qN(v1)`、`qN(v2)`、`qN(v3)` 或 `qN(final)` 前缀，后接简明中文说明。

## 交付前检查

1. 检查变更范围，确认没有覆盖或混入无关改动。
2. 运行与风险相匹配的最小有效验证，记录实际命令和结果。
3. 检查代码参数、报告公式、结果表、图表和论文关键数字是否一致。
4. 列出已完成内容、验证证据、剩余风险和未执行操作，不得用计划或推测代替完成证据。
